{
  "name": "styleme-root",
  "private": true,
  "description": "Root npm scripts for the StyleMe web app in ./web.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "install:web": "npm install --prefix web"
  }
}
