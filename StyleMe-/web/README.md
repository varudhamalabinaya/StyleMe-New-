# StyleMe (web)

Vite + React + TypeScript app. See the repository root [README.md](../README.md) for flow, Supabase, design system, **draft persistence**, and **Back / stepper** behavior.

```bash
npm install
npm run dev
```
