# Copy to .env and set your machine's LAN IP when testing on a physical device.
# Android emulator can use the default (10.0.2.2). iOS simulator uses localhost.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:8080
